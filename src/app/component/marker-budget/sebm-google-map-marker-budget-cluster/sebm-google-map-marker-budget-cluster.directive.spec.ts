import { SebmGoogleMapMarkerBudgetClusterDirective } from './sebm-google-map-marker-budget-cluster.directive';

describe('SebmGoogleMapMarkerBudgetClusterDirective', () => {
  it('should create an instance', () => {
    const directive = new SebmGoogleMapMarkerBudgetClusterDirective();
    expect(directive).toBeTruthy();
  });
});
